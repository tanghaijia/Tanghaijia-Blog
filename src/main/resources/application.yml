spring:
  thymeleaf:
    mode: HTML5
  profiles:
    active: dev
  messages:
    basename: i18n/messages

  data:
    rest:
      base-path: /api

  datasource:
    url: jdbc:mysql://localhost/myblog?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: thj13835720054
    driver-class-name: com.mysql.cj.jdbc.Driver

  redis:
    database: 0
    #redis服务器地址
    host: localhost
    #redis连接端口号
    port: 6379
    #redis连接密码
    password:
    #redis连接超时时间（毫秒）
    timeout: 18000
    lettuce:
      pool:
        # 连接池最小空闲连接
        min-idle: 1
        # 连接池最大空闲连接
        max-idle: 10
        # 连接池最大阻塞等待时间（使用负值表示没有限制）
        max-wait: -1
        # 连接池最大连接数（使用负值表示没有限制）
        max-active: -1

comment.avatar: /images/avatar.png


